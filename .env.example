# MyMove Complete Environment Configuration
# ==========================================
# Copy this file to .env and adjust values

# ============================================================
# PORTS (change if already in use)
# ============================================================
MYSQL_PORT=3306
BACKEND_PORT=8080
FRONTEND_PORT=80

# ============================================================
# DATABASE CONFIGURATION
# ============================================================
MYSQL_DATABASE=mymove
MYSQL_USER=mymove
MYSQL_PASSWORD=mymove
MYSQL_ROOT_PASSWORD=root

# ============================================================
# BACKEND CONFIGURATION
# ============================================================

# Spring Profile (docker, mock-ai, prod)
SPRING_PROFILES_ACTIVE=docker,mock-ai

# JPA DDL Mode
# - update: Auto-create/update schema (development)
# - validate: Validate schema, don't modify (production)
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# Admin Credentials
# ⚠️ CHANGE THESE IN PRODUCTION!
MYMOVE_ADMIN_EMAIL=admin@mymove.at
MYMOVE_ADMIN_PASSWORD=admin123

# ============================================================
# FRONTEND CONFIGURATION
# ============================================================

# API URL that frontend should connect to
# In Docker: http://localhost:8080/api/v1 (when using mapped port)
# For external access: http://your-domain:8080/api/v1
VITE_API_BASE_URL=http://localhost:8080/api/v1

# ============================================================
# AI SERVICE (future use)
# ============================================================
AI_SERVICE_URL=http://mock-ai:8000
