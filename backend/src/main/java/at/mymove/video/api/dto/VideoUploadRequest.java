package at.mymove.video.api.dto;

/**
 * Placeholder for future metadata (e.g. room count, move size, notes).
 * Aktuell leer – bewusst.
 */
public record VideoUploadRequest(
        String description
) {
}